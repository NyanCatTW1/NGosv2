// Program codename: Program name, Program cost, Program description
const storeProgramList = {
  browser: ["Web browser", new Decimal("0.1"), "A web browser so you can actually browse things online freedomly, it's on sale and nobody can live without it, so why not?", 0]
}
